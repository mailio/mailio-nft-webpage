import { Box, Typography } from '@mui/material';
import { FC } from 'react';

export const AccountNFTs: FC = () => {
    return (
        <Box>
            <Typography>
                NFTs
            </Typography>
        </Box>
    )
};